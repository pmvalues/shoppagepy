// Ambient declaration for the webpack-provided `__non_webpack_require__` global
// used by the kernel's Node.js SQLite access paths. It is available at runtime
// (or falls back to eval('require')) but is not present in the TypeScript scope.
declare const __non_webpack_require__: any;
