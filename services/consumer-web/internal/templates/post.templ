package templates

import (
	"github.com/shoppage/consumer-web/internal/models"
)

// NewPostCardComponent renders a just-published Trade Wire post for HTMX.
templ NewPostCardComponent(post models.PostItem) {
	@PostCard(post)
}
